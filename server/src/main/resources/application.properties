apikey=${APIKEY}

# Local MySQL settings
# spring.datasource.url=jdbc:mysql://localhost/testdb
# spring.datasource.username=root
# spring.datasource.password=${MYSQLPASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MYSQL RAILWAY SETTINGS:
spring.datasource.url=${MYSQL_APP_URL}
spring.datasource.username=${MYSQL_APP_USER}
spring.datasource.password=${MYSQL_APP_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# App Properties
app.jwtSecret= ${JWTSECRET}
app.jwtExpirationMs= 86400000

# GMAIL SETTINGS
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=foodalchemytfip@gmail.com
spring.mail.password=${GMAIL_APP_PASSWORD}
spring.mail.properties.mail.debug=true

spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Local MongoDB settings
mongo_url=${MONGO_URL}
# spring.data.mongodb.host=localhost
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=recipe

spring.data.mongodb.host=${MONGOHOST}
spring.data.mongodb.port=6443
spring.data.mongodb.username=${MONGOUSER}
spring.data.mongodb.password=${MONGOPASSWORD}

spring.security.oauth2.client.registration.google.client-id=593942135022-q3so6hmf710t9pr93q0agqo9r5n4l6vb.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_SECRET}
